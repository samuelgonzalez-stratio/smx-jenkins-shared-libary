pipeline {
    agent any
    options {
        // skipStagesAfterUnstable()
    }
    stages {
        stage('Build') {
            steps {
                echo "Hello Jenkins Pipeline!"
            }
        }
    }
}